language: node_js
node_js:
  - "stable"
dist: trusty
services:
  - mysql
  - redis-server
env:
  - NODE_ENV=test PORT=3000
cache:
  directories:
    - node_modules
before_install:
  - mysql -e 'CREATE DATABASE IF NOT EXISTS database_test;'
install: 
  - npm install
script:
  - nohup npm run start &
  - sleep 3
  - npm run test


